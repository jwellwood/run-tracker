const profile = require('./profile-messages');
const record = require('./record-messages');
const registration = require('./registration-messages');
const server = require('./server-messages');

module.exports = { profile, record, registration, server };
