const profile_messages = require('./profile-messages');
const act_mess = require('./activity-messages');
const registration = require('./registration-messages');
const server = require('./server-messages');

module.exports = { profile_messages, act_mess, registration, server };
